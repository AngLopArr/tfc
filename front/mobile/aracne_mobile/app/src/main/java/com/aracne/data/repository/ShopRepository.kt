package com.aracne.data.repository

import com.aracne.data.remote.RetrofitInstance
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/**
 * Repositorio que gestiona la obtención y manipulación de datos en la aplicación.
 *
 * Se encarga de realizar las llamadas a la API del laboratorio para obtener, modificar y eliminar
 * materiales, préstamos, incidencias, notificaciones y gestionar la autenticación del usuario.
 */
class ShopRepository { }